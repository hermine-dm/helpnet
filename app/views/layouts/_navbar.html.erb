  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">
      HELPNET
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item text-uppercase px-3 active">
          <%= link_to "Accueil", root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item text-uppercase px-3 active">
          <%= link_to "Forum", root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item text-uppercase px-3 active">
          <%= link_to "Articles", root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item text-uppercase px-3 active">
          <%= link_to "Marketplace", root_path, class: 'nav-link' %>
        </li>


        <% if user_signed_in? == false %>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item dropdown">
                <a class="dropdown-toggle btn btn-outline-secondary mx-3 text-uppercase" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  S'inscrire / Se connecter
                </a>
                <!-- Here's the magic. Add the .animate and .slide-in classes to your .dropdown-menu and you're all set! -->
                <div class="dropdown-menu dropdown-menu-right animate slideIn" aria-labelledby="navbarDropdown">
                  <%= link_to "S'inscrire", new_user_registration_path, class: 'dropdown-item text-uppercase' %>
                  <div class="dropdown-divider"></div>
                  <%= link_to "Se Connecter", new_user_session_path, class: 'dropdown-item text-uppercase' %>
                </div>
              </li>
            </ul>
          </div>
        <% else %>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle btn btn-outline-primary text-light text-uppercase" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Mon Profil
                </a>
                <!-- Here's the magic. Add the .animate and .slide-in classes to your .dropdown-menu and you're all set! -->
                <div class="dropdown-menu dropdown-menu-right animate slideIn" aria-labelledby="navbarDropdown">
                  <%= link_to "Mon Profil", user_path(current_user.id), class: 'dropdown-item text-uppercase' %>
                  <div class="dropdown-divider"></div>
                  <%= link_to "Se Déconnecter", destroy_user_session_path, method: :delete, class: 'dropdown-item text-uppercase' %>
                </div>
              </li>
            </ul>
          </div>

        <% end %>
    </ul>
  </div>
  </nav>
