<%= form_for [@article,@comment] do |f| %>
    <div class="form-group">   
        <%= f.text_field :content, class:'form_control' %>
    </div>
    <div class="form-group">
        <%= submit_tag "Modifier", class: 'btn btn-primary btn-sm btn-submit'  %>
    </div>
<% end %>