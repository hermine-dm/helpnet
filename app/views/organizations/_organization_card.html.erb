<div class="col-md-3 m-4">
<!-- ***** Popover information && Template***** -->
  <div class="row shadow p-3 usertype-cardmodel" data-container="body" data-toggle="popover" data-placement="top" data-content="<%= organization.description%>" data-template='<div class="popover" role="tooltip"><div class="arrow"></div><p class="popover-body"></p></div>'>

    <!-- ***** Logo ***** -->
    <div class="col-md-4 text-center my-auto">
      <%if organization.logo.attached?%>
         <%= image_tag organization.logo, alt: "logo", class:"rounded-circle", height:"100", width:"100" %>
      <%else%>
          <%= image_tag "mains.jpg", alt:"", class:"rounded-circle", height:"120", width:"120" %>
      <%end%>
    </div>
    <div class="col-md-8">
      <!-- ***** Name ***** -->
      <h4 class="text-md-left text-center">
        <%= organization.name %>
      </h4>
      <!-- ***** Location ***** -->
      <p class="my-4">
        <i class="fas fa-map-marker-alt"></i> <%= organization.location %>
      </p>
      <!-- ***** Follow and see more buttons ***** -->
      <div class="row text-md-left text-center">
        <div class="col-6">
          <%if current_user%>
            <% @follow = Follow.find_by(user_id:current_user.id, organization_id:organization.id)%>
            <%if @follow == nil%>
              <%= link_to "Suivre", organization_follows_path(organization.id), method: :post, :class => "btn btn-outline-secondary"  %>
            <%else%>
              <%= link_to "Ne plus suivre", organization_follow_path(organization.id, @follow.id), method: :delete, :class => "btn btn-outline-secondary" %>
            <%end%>
          <%else%>
              <%= link_to "Suivre", new_user_session_path, :class => "btn btn-outline-secondary"%>
          <%end%>
        </div>
        <div class="col-6">
          <%= link_to "Découvrir", organization_path(organization.id), class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
</div>
